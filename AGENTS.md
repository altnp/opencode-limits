# OpenCode Limits CLI

This repo contains the `opencode-limits` CLI for viewing Codex and Copilot usage.

## Commands
- `uv run opencode-limits --help`
- `uv run opencode-limits`
- `uv run python -m pytest`

## Testing Standards
- Keep tests fast and deterministic.
- Avoid live API calls; mock HTTP responses instead.
